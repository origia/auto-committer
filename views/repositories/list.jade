.repositories(ng-controller="RepoCtrl")
  a.rep(href="#" ng-repeat="repository in repos")
    .title
      h2= "{{repository.title}}"
    .metadata
      p.path= "{{repository.path}}"
      p.percentage 進捗
        span= "{{repository.progress}}%"

.story-progress
  p.percentage {{repository.progress}}%
  p.bar
    progress(max='100' value='{{repository.progress}}')
  p.todo
    | タスク
    span {{tasks.done.length}}
    | /{{getTasks().length}}
  div(ng-if="next.task != null")
    p.next {{next.task.content}}
    p.will
      | を完了すると……
      span {{next.progress}}%
  .option
    p.open-finder(ng-click="openDir(repository.workingDir())") {{openText}}
